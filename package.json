{
  "name": "api-contracts",
  "version": "0.0.2",
  "main": "index.ts",
  "scripts": {
    "generate": "swagger-typescript-api generate --path ./openapi.json --output ./generated -t ./src --modular -r",
    "watch": "nodemon --watch openapi.json --exec \"npm run generate\"",
    "docs": "npx serve docs"
  },
  "devDependencies": {
    "serve": "^14.2.6",
    "swagger-typescript-api": "^13.6.10"
  },
  "dependencies": {
    "neverthrow": "^8.2.0"
  }
}
